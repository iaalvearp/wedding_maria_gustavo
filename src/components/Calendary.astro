---

---

<section>
    <picture class="pic-box">
        <img class="pic-calendary" src="img/calendar_cr.webp" alt="">
    </picture>
    <img class="left-calendar-flowers" src="assets/left-calendar-flowers.png" alt="">
    <img class="watercolor-calendar-right" src="assets/watercolor-calendar-right.png" alt="">
    <img class="date" src="assets/date-title.png" alt="">
    <img class="calendar" src="assets/calendario.png" alt="">
    <p>Queremos compartir contigo este día especial<br>con quienes han sido parte de nuestra<br>historia y nuestro amor.</p>
    <img class="wcolor-left" src="assets/watercolor-places-left.png" alt="">
    <img class="wcolor-right" src="assets/watercolor-places-right.png" alt="">
</section>

<style>
    section {
        width: 100%;
        max-width: 48rem;
        margin: 0 auto;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        color: var(--brown);
        position: relative;

        .pic-box {
            width: 100%;
            height: 12rem;
            display: block;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
    
        .pic-calendary {
            object-fit: cover;
            width: 120%;
            height: auto;
            position: absolute;
            right: 0;
        }
    
        .left-calendar-flowers {
            width: 30vw;
            POSITION: absolute;
            left: -1.25rem;
            top: 12rem;
        }
    
        .watercolor-calendar-right {
            width: 30vw;
            position: absolute;
            right: -4rem;
            top: 8rem;
        }
    
        .date {
            margin: 0 auto;
            margin-top: 3rem;
            width: 55vw;
        }
    
        .calendar {
            width: 60vw;
            margin: 0 auto;
            margin-top: 4rem;
        }
    
        p {
            font-size: 1rem;
            line-height: 1.5rem;
            letter-spacing: 0.5px;
            text-align: center;
            margin: 3rem 0;
            font-family: 'Geliat', sans-serif;
        }

        .wcolor-left, .wcolor-right {
            position: absolute;
            left: -14rem;
            bottom: -11rem;
            width: 100vw;
            max-width: 48rem;
            opacity: 0.4;
            z-index: -1;
        }
        
        .wcolor-right {
            bottom: -12rem;
            left: unset;
            right: -14rem;
        }
    }

    @media (width >= 768px) {

        section {

            .pic-box {
                height: 22rem;
            }

            .pic-calendary {
                width: 100%;
            }

            .calendar {
                margin-top: 8rem;
            }

            .left-calendar-flowers, .watercolor-calendar-right {
                top: 21rem;
                max-width: 230px;
            }

            .left-calendar-flowers {
                left: -1.75rem;
            }

            .watercolor-calendar-right {
                right: -6rem;
            }

            .date {
                margin-top: 5rem;
                max-width: 25rem;
            }

            .calendar {
                max-width: 29rem;
            }

            p {
                font-size: 1.25rem;
                line-height: 1.75rem;
                letter-spacing: 1px;
                margin-top: 4em;
            }

            .wcolor-left {
                left: -23rem;
                bottom: -20rem;
            }
            
            .wcolor-right {
                bottom: -21rem;
                left: unset;
                right: -23rem;
            }
        }
    }
</style>
